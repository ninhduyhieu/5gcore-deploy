---
- name: Apply ETRI 5GC with Helm after registry is ready
  hosts: "{{ node_name }}"
  become: true
  gather_facts: true
  vars_files:
    - /vagrant/ansible/vars.yml
    - /vagrant/ansible/vars_helm.yml
  roles:
    - label_nodes
    - deploy_core_helm
