---
- name: Configure node containerd to use local registry
  hosts: "{{ node_name }}"
  become: true
  gather_facts: true
  vars_files:
    - vars.yml
    - vars_helm.yml
  roles:
    - registry_clients
