name: Production-Style Infrastructure and Application CI/CD

on:
  workflow_dispatch:
    inputs:
      image_owner:
        description: "GHCR owner"
        required: true
        default: "ninhduyhieu"
      image_tag:
        description: "Image tag"
        required: true
        default: "latest"

  push:
    branches: [main, develop]

# LOGIC: cancel-in-progress=true an toàn vì infra-ensure reuse VM (không destroy).
# Nếu cancel giữa chừng khi đang vagrant up, lần chạy tiếp theo sẽ resume VM.
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ─────────────────────────────────────────────────────────────────────────────
  validate:
  # ─────────────────────────────────────────────────────────────────────────────
    name: "CI · Validate Syntax"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # ubuntu-latest dùng python3. pip3 + --break-system-packages tránh
      # lỗi "externally-managed-environment" trên Ubuntu 22.04+.
      - name: Install validation tools
        run: pip3 install --break-system-packages yamllint ansible

      - name: Lint YAML files
        run: |
          yamllint -d "{extends: relaxed, rules: {line-length: {max: 220}}}" \
            .github/workflows/build-and-deploy-local-source.yml \
            k8s/ observability/ roles/ playbook.yml

      - name: Check Ansible playbook syntax
        run: ansible-playbook --syntax-check -i inventory/hosts.yaml playbook.yml

  # ─────────────────────────────────────────────────────────────────────────────
  detect-changes:
  # ─────────────────────────────────────────────────────────────────────────────
    name: "CI · Detect Changes"
    runs-on: ubuntu-latest
    outputs:
      need_infra:      ${{ steps.detect.outputs.need_infra }}
      need_build:      ${{ steps.detect.outputs.need_build }}
      need_deploy:     ${{ steps.detect.outputs.need_deploy }}
      need_monitoring: ${{ steps.detect.outputs.need_monitoring }}
      need_webui:      ${{ steps.detect.outputs.need_webui }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect changed areas
        id: detect
        shell: bash
        run: |
          set -e

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "need_infra=true"      >> "$GITHUB_OUTPUT"
            echo "need_build=true"      >> "$GITHUB_OUTPUT"
            echo "need_deploy=true"     >> "$GITHUB_OUTPUT"
            echo "need_monitoring=true" >> "$GITHUB_OUTPUT"
            echo "need_webui=true"      >> "$GITHUB_OUTPUT"
            exit 0
          fi

          CHANGED=$(git diff --name-only HEAD~1 HEAD 2>/dev/null || true)
          echo "Changed files:"
          echo "$CHANGED"

          NEED_INFRA=false
          NEED_BUILD=false
          NEED_DEPLOY=false
          NEED_MONITORING=false
          NEED_WEBUI=false

          echo "$CHANGED" | grep -qE '(^Vagrantfile$|^inventory/|^roles/|^playbook\.yml$)' && NEED_INFRA=true      || true
          echo "$CHANGED" | grep -qE '(^docker/|nf\.Dockerfile$)'                          && NEED_BUILD=true      || true
          echo "$CHANGED" | grep -qE '(^k8s/|^scripts/render-ghcr-images\.sh$)'            && NEED_DEPLOY=true     || true
          echo "$CHANGED" | grep -qE '^observability/'                                      && NEED_MONITORING=true || true
          echo "$CHANGED" | grep -qE '^webui/'                                              && NEED_WEBUI=true      || true

          # Build mới → phải deploy lại image mới
          if [ "$NEED_BUILD" = "true" ]; then
            NEED_DEPLOY=true
          fi

          echo "need_infra=$NEED_INFRA"           >> "$GITHUB_OUTPUT"
          echo "need_build=$NEED_BUILD"           >> "$GITHUB_OUTPUT"
          echo "need_deploy=$NEED_DEPLOY"         >> "$GITHUB_OUTPUT"
          echo "need_monitoring=$NEED_MONITORING" >> "$GITHUB_OUTPUT"
          echo "need_webui=$NEED_WEBUI"           >> "$GITHUB_OUTPUT"

  # ─────────────────────────────────────────────────────────────────────────────
  infra-ensure:
  # ─────────────────────────────────────────────────────────────────────────────
    # LOGIC: infra-ensure LUÔN chạy với mọi push/dispatch vì:
    # 1. Cluster health check cần xác nhận VM alive trước khi deploy.
    # 2. SSH config + Ansible inventory phải được regenerate mỗi lần
    #    (VM có thể đã restart, port key có thể đổi).
    # Các step tốn kém (Ansible apply, APT) có guard `if: need_infra == 'true'`.
    name: "CD · Ensure Infra Healthy"
    runs-on: [self-hosted, linux, x64]
    needs: [validate, detect-changes]
    env:
      INFRA_DIR: ${{ vars.INFRA_DIR || '' }}
    outputs:
      image_owner: ${{ steps.vars.outputs.owner }}
      image_tag:   ${{ steps.vars.outputs.tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Resolve paths
        shell: bash
        run: |
          echo "INFRA_DIR=${INFRA_DIR:-$HOME/5gcore-deploy}" >> "$GITHUB_ENV"

      # rsync --delete + đủ exclude để không xóa runtime files.
      # rendered/ và rendered-test/ exclude vì chúng được tạo in-place trên runner,
      # không có trong GITHUB_WORKSPACE nên --delete sẽ xóa mất.
      - name: Sync latest infra code
        shell: bash
        run: |
          set -e
          mkdir -p "$INFRA_DIR"
          rsync -a --delete \
            --exclude '.git/' \
            --exclude '.vagrant/' \
            --exclude '.venv/' \
            --exclude 'kubeconfig' \
            --exclude '.vagrant-ssh-config' \
            --exclude '.ansible-vagrant-inventory.yaml' \
            --exclude 'rendered/' \
            --exclude 'rendered-test/' \
            "$GITHUB_WORKSPACE"/ "$INFRA_DIR"/

      - name: Generate image tag
        id: vars
        shell: bash
        run: |
          set -e

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            OWNER="${{ inputs.image_owner }}"
            if [ "${{ inputs.image_tag }}" = "latest" ]; then
              TAG="manual-$(date +%Y%m%d-%H%M%S)"
            else
              TAG="${{ inputs.image_tag }}"
            fi
          else
            OWNER="ninhduyhieu"
            TAG="sha-$(echo ${{ github.sha }} | cut -c1-7)"
          fi

          [ -z "$OWNER" ] && OWNER="ninhduyhieu"
          [ -z "$TAG"   ] && TAG="latest"

          echo "OWNER=$OWNER  TAG=$TAG"
          echo "owner=$OWNER" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG"     >> "$GITHUB_OUTPUT"

      - name: Ensure Vagrant VMs are running
        shell: bash
        run: |
          set -e
          cd "$INFRA_DIR"

          echo "=== Vagrant status before boot ==="
          vagrant status || true

          # ── BƯỚC 1: Sync VirtualBox state về Vagrant ──────────────────────
          # Khi job bị cancel giữa chừng hoặc host tắt đột ngột, VirtualBox
          # vẫn còn VM nhưng .vagrant/ mất track → Vagrant báo "not_created"
          # trong khi VBoxHeadless đang giữ port. Phát hiện và fix tự động:
          for vbox_vm in "5gcore-deploy_k3s-server-1" "5gcore-deploy_k3s-agent-1" "5gcore-deploy_k3s-agent-2"; do
            vbox_state=$(VBoxManage showvminfo "$vbox_vm" --machinereadable 2>/dev/null               | awk -F'"' '/^VMState=/{print $2}')
            if [ -n "$vbox_state" ]; then
              echo "VirtualBox: $vbox_vm = $vbox_state"
              if [ "$vbox_state" = "running" ] || [ "$vbox_state" = "starting" ]; then
                echo "  → Force poweroff $vbox_vm (orphan VM)"
                VBoxManage controlvm "$vbox_vm" poweroff || true
                sleep 3
              fi
            fi
          done

          # ── BƯỚC 2: Kill VBoxHeadless còn giữ port SSH ────────────────────
          for port in 2200 2201 2202; do
            pid=$(sudo ss -tlnp 2>/dev/null               | awk -v p=":${port} " '$0 ~ p {match($0,/pid=([0-9]+)/,a); print a[1]}')
            if [ -n "$pid" ]; then
              echo "Port $port held by PID $pid — killing..."
              sudo kill -9 "$pid" || true
            fi
          done
          sleep 3

          # Verify port sạch
          for port in 2200 2201 2202; do
            if ss -tlnp 2>/dev/null | grep -q ":${port} "; then
              echo "ERROR: port $port still in use after cleanup"
              exit 1
            fi
          done
          echo "All SSH ports are free."

          # ── BƯỚC 3: Xử lý Vagrant state ───────────────────────────────────
          for vm in k3s-server-1 k3s-agent-1 k3s-agent-2; do
            state=$(vagrant status "$vm" --machine-readable               | awk -F, '/,state,/{print $4}' | tail -1)
            echo "$vm vagrant state: ${state:-unknown}"
            if [ "$state" = "aborted" ] || [ "$state" = "saved" ]; then
              vagrant halt -f "$vm" || true
            fi
          done

          # ── BƯỚC 4: Up ────────────────────────────────────────────────────
          # Reuse VM nếu đã tồn tại — không destroy để giữ k3s state.
          timeout 20m vagrant up --no-parallel

      # Dùng printf thay heredoc — heredoc trong YAML run block bị indent
      # theo cấu trúc YAML, làm SSH config parser đọc sai (Host bị thụt vào).
      # HostName = 127.0.0.1 (không dùng alias VM name).
      - name: Generate SSH config
        shell: bash
        run: |
          set -e
          cd "$INFRA_DIR"

          get_port() { vagrant ssh-config "$1" 2>/dev/null | awk '/^\s*Port /{print $2; exit}'; }
          # Vagrant có thể liệt kê nhiều IdentityFile (ed25519 + rsa) trên nhiều dòng.
          # Lấy dòng đầu tiên (ed25519) để $GITHUB_ENV không bị "Invalid format".
          # SSH config file sẽ liệt kê cả hai key để fallback hoạt động đúng.
          get_key()  { vagrant ssh-config "$1" 2>/dev/null | awk '/^\s*IdentityFile /{print $2; exit}'; }
          # Lấy tất cả IdentityFile của một VM (để ghi vào SSH config file)
          get_keys() { vagrant ssh-config "$1" 2>/dev/null | awk '/^\s*IdentityFile /{print $2}'; }

          PORT_SERVER1=$(get_port k3s-server-1)
          PORT_AGENT1=$(get_port  k3s-agent-1)
          PORT_AGENT2=$(get_port  k3s-agent-2)
          KEY_SERVER1=$(get_key   k3s-server-1)
          KEY_AGENT1=$(get_key    k3s-agent-1)
          KEY_AGENT2=$(get_key    k3s-agent-2)

          for var in PORT_SERVER1 PORT_AGENT1 PORT_AGENT2 KEY_SERVER1 KEY_AGENT1 KEY_AGENT2; do
            val=$(eval echo "\$$var")
            if [ -z "$val" ]; then
              echo "ERROR: failed to parse $var from vagrant ssh-config"
              exit 1
            fi
          done
          echo "Ports: server=$PORT_SERVER1  agent1=$PORT_AGENT1  agent2=$PORT_AGENT2"

          # Hàm ghi block SSH config cho một VM — IdentityFile ghi từng dòng riêng
          write_ssh_block() {
            local vm=$1 port=$2
            printf 'Host %s\n' "$vm"
            printf '  HostName 127.0.0.1\n'
            printf '  User vagrant\n'
            printf '  Port %s\n' "$port"
            get_keys "$vm" | while IFS= read -r keyfile; do
              printf '  IdentityFile %s\n' "$keyfile"
            done
            printf '  StrictHostKeyChecking no\n'
            printf '  UserKnownHostsFile /dev/null\n'
            printf '  PasswordAuthentication no\n'
            printf '  LogLevel FATAL\n'
            printf '\n'
          }

          {
            write_ssh_block "k3s-server-1" "$PORT_SERVER1"
            write_ssh_block "k3s-agent-1"  "$PORT_AGENT1"
            write_ssh_block "k3s-agent-2"  "$PORT_AGENT2"
          } > "$INFRA_DIR/.vagrant-ssh-config"

          chmod 600 "$INFRA_DIR/.vagrant-ssh-config"
          echo "=== Generated SSH config ==="
          cat "$INFRA_DIR/.vagrant-ssh-config"

          echo "PORT_SERVER1=$PORT_SERVER1" >> "$GITHUB_ENV"
          echo "PORT_AGENT1=$PORT_AGENT1"   >> "$GITHUB_ENV"
          echo "PORT_AGENT2=$PORT_AGENT2"   >> "$GITHUB_ENV"
          echo "KEY_SERVER1=$KEY_SERVER1"   >> "$GITHUB_ENV"
          echo "KEY_AGENT1=$KEY_AGENT1"     >> "$GITHUB_ENV"
          echo "KEY_AGENT2=$KEY_AGENT2"     >> "$GITHUB_ENV"

      # Retry 10 lần — SSH daemon chưa sẵn sàng ngay sau khi VM boot.
      # StrictHostKeyChecking=no tránh "REMOTE HOST IDENTIFICATION HAS CHANGED"
      # khi VM restart và đổi host key.
      - name: Verify SSH connectivity with retry
        shell: bash
        run: |
          set -e

          for vm in k3s-server-1 k3s-agent-1 k3s-agent-2; do
            echo "--- Testing SSH to $vm ---"
            ok=false
            for attempt in $(seq 1 10); do
              if ssh \
                   -F "$INFRA_DIR/.vagrant-ssh-config" \
                   -o StrictHostKeyChecking=no \
                   -o UserKnownHostsFile=/dev/null \
                   -o ConnectTimeout=10 \
                   "$vm" "echo SSH_OK_$vm"; then
                ok=true
                break
              fi
              echo "  Attempt $attempt/10 failed — waiting 15s..."
              sleep 15
            done
            if [ "$ok" = "false" ]; then
              echo "ERROR: cannot SSH into $vm after 10 attempts."
              exit 1
            fi
          done

      # ansible_host = 127.0.0.1 + ansible_port tường minh thay vì alias VM.
      # ansible_ssh_common_args là chuỗi đơn (không dùng YAML >- block
      # vì >- có thể sinh newline và break ansible argument parsing).
      - name: Generate Ansible inventory
        shell: bash
        run: |
          set -e
          cd "$INFRA_DIR"

          printf '%s\n' \
            "all:" \
            "  hosts:" \
            "    k3s-server-1:" \
            "      ansible_host: 127.0.0.1" \
            "      ansible_port: ${PORT_SERVER1}" \
            "      ansible_user: vagrant" \
            "      ansible_ssh_private_key_file: ${KEY_SERVER1}" \
            "      ansible_python_interpreter: /usr/bin/python3" \
            "      ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'" \
            "      node_ip: 192.168.58.11" \
            "    k3s-agent-1:" \
            "      ansible_host: 127.0.0.1" \
            "      ansible_port: ${PORT_AGENT1}" \
            "      ansible_user: vagrant" \
            "      ansible_ssh_private_key_file: ${KEY_AGENT1}" \
            "      ansible_python_interpreter: /usr/bin/python3" \
            "      ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'" \
            "      node_ip: 192.168.58.12" \
            "    k3s-agent-2:" \
            "      ansible_host: 127.0.0.1" \
            "      ansible_port: ${PORT_AGENT2}" \
            "      ansible_user: vagrant" \
            "      ansible_ssh_private_key_file: ${KEY_AGENT2}" \
            "      ansible_python_interpreter: /usr/bin/python3" \
            "      ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'" \
            "      node_ip: 192.168.58.13" \
            "  children:" \
            "    masters:" \
            "      hosts:" \
            "        k3s-server-1:" \
            "    workers:" \
            "      hosts:" \
            "        k3s-agent-1:" \
            "        k3s-agent-2:" \
            > .ansible-vagrant-inventory.yaml

          echo "=== Generated Ansible inventory ==="
          cat .ansible-vagrant-inventory.yaml

      - name: Release APT locks on VMs
        if: needs.detect-changes.outputs.need_infra == 'true'
        shell: bash
        run: |
          set -e
          cd "$INFRA_DIR"

          for vm in k3s-server-1 k3s-agent-1 k3s-agent-2; do
            vagrant ssh "$vm" -c \
              "sudo killall -9 apt-get apt dpkg 2>/dev/null || true; \
               sudo rm -f /var/lib/dpkg/lock-frontend \
                          /var/lib/apt/lists/lock \
                          /var/lib/dpkg/lock; \
               sudo dpkg --configure -a || true" || true
          done

      - name: Apply Ansible configuration
        if: needs.detect-changes.outputs.need_infra == 'true'
        shell: bash
        run: |
          set -e
          cd "$INFRA_DIR"

          ANSIBLE_HOST_KEY_CHECKING=False \
          ansible-playbook \
            -i .ansible-vagrant-inventory.yaml \
            playbook.yml -vv

      # patch_ssh_entry dùng awk thuần — không dùng python3 -c multiline
      # (gây lỗi YAML parser vì code Python ở cột 0 bị đọc như YAML key).
      #
      # LOGIC sleep 120: chỉ sleep khi thực sự có VM được restart.
      # Nếu tất cả VM đều Ready → skip sleep, tiết kiệm 2 phút mỗi lần push.
      - name: Cluster health check with auto-heal
        shell: bash
        run: |
          set -e
          export KUBECONFIG="$INFRA_DIR/kubeconfig"
          cd "$INFRA_DIR"

          echo "Checking K3s API /cacerts..."
          curl -k --fail https://192.168.58.11:6443/cacerts >/dev/null

          echo "Initial node state..."
          kubectl get nodes -o wide || true

          patch_ssh_entry() {
            local vm=$1
            local port cfg
            cfg="$INFRA_DIR/.vagrant-ssh-config"
            port=$(vagrant ssh-config "$vm" 2>/dev/null | awk '/^\s*Port /{print $2; exit}')
            [ -z "$port" ] && echo "WARNING: could not get port for $vm" && return

            # Xoá block cũ của VM này
            awk -v vm="$vm" '/^Host / { in_block = ($2 == vm) } !in_block { print }' \
              "$cfg" | sed '/^$/N;/^\n$/d' > "${cfg}.tmp"

            # Ghi block mới với tất cả IdentityFile (ed25519 + rsa nếu có)
            printf '\nHost %s\n  HostName 127.0.0.1\n  User vagrant\n  Port %s\n' \
              "$vm" "$port" >> "${cfg}.tmp"
            vagrant ssh-config "$vm" 2>/dev/null \
              | awk '/^\s*IdentityFile /{print "  IdentityFile " $2}' >> "${cfg}.tmp"
            printf '  StrictHostKeyChecking no\n  UserKnownHostsFile /dev/null\n  PasswordAuthentication no\n  LogLevel FATAL\n' \
              >> "${cfg}.tmp"

            mv "${cfg}.tmp" "$cfg"
            chmod 600 "$cfg"
            echo "Patched SSH config for $vm (port=$port)"
          }

          healed=false
          for vm in k3s-agent-1 k3s-agent-2; do
            if ! kubectl get node "$vm" \
                   -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}' \
                   2>/dev/null | grep -q True; then
              echo "$vm is NotReady → restart VM and k3s-agent"
              vagrant halt -f "$vm" || true
              timeout 10m vagrant up "$vm" --no-provision
              patch_ssh_entry "$vm"
              vagrant ssh "$vm" -c \
                "sudo systemctl restart k3s-agent || sudo systemctl restart k3s || true"
              healed=true
            fi
          done

          # Chỉ sleep khi thực sự có VM vừa được restart
          if [ "$healed" = "true" ]; then
            echo "Waiting for kubelet and Calico to settle after heal..."
            sleep 120
          fi

          echo "Final node state..."
          kubectl get nodes -o wide          || true
          kubectl get pods -n kube-system -o wide || true

          kubectl wait --for=condition=Ready node/k3s-server-1 --timeout=600s
          kubectl wait --for=condition=Ready node/k3s-agent-1  --timeout=900s
          kubectl wait --for=condition=Ready node/k3s-agent-2  --timeout=900s

          kubectl wait \
            --for=condition=Ready pod \
            -n kube-system \
            -l k8s-app=calico-node \
            --timeout=900s

          kubectl get nodes -o wide
          kubectl get pods -A -o wide

  # ─────────────────────────────────────────────────────────────────────────────
  pre-deploy-test:
  # ─────────────────────────────────────────────────────────────────────────────
    name: "CI · Pre-Deploy Test"
    runs-on: [self-hosted, linux, x64]
    needs: [infra-ensure, detect-changes]
    env:
      INFRA_DIR: ${{ vars.INFRA_DIR || '' }}
    steps:
      - uses: actions/checkout@v4

      - name: Resolve paths
        shell: bash
        run: |
          echo "INFRA_DIR=${INFRA_DIR:-$HOME/5gcore-deploy}" >> "$GITHUB_ENV"

      - name: Run smoke tests before deploy
        shell: bash
        run: |
          set -e
          export KUBECONFIG="$INFRA_DIR/kubeconfig"
          cd "$INFRA_DIR"

          echo "=== Cluster connectivity ==="
          kubectl get nodes -o wide
          kubectl get pods -n kube-system -o wide

          # LOGIC: manifest check và dry-run chỉ cần thiết khi sẽ deploy.
          # Nếu chỉ commit webui/monitoring thì không cần validate k8s manifest.
          if [ "${{ needs.detect-changes.outputs.need_deploy }}" = "true" ]; then
            echo "=== Check manifests exist ==="
            test -f k8s/ctrl.yaml
            test -f k8s/gw.yaml
            test -f k8s/nsm.yaml
            test -f k8s/mongo.yaml
            test -f k8s/deploy-other.yaml
            test -f k8s/pran.yaml
            test -x scripts/render-ghcr-images.sh

            echo "=== Dry-run render/apply manifests ==="
            OWNER="${{ needs.infra-ensure.outputs.image_owner }}"
            TAG="${{ needs.infra-ensure.outputs.image_tag }}"
            [ -z "$OWNER" ] && OWNER="ninhduyhieu"
            [ -z "$TAG"   ] && TAG="latest"

            ./scripts/render-ghcr-images.sh "$OWNER" "$TAG" rendered-test

            kubectl create namespace etri6g \
              --dry-run=client -o yaml | kubectl apply -f -

            for f in ctrl gw nsm mongo deploy-other pran; do
              echo "Validating rendered-test/${f}.yaml"
              kubectl apply --dry-run=server -f "rendered-test/${f}.yaml"
            done
          fi

  # ─────────────────────────────────────────────────────────────────────────────
  app-delivery:
  # ─────────────────────────────────────────────────────────────────────────────
    name: "CD · Build & Deploy Application"
    runs-on: [self-hosted, linux, x64]
    needs: [infra-ensure, detect-changes, pre-deploy-test]
    env:
      INFRA_DIR:  ${{ vars.INFRA_DIR  || '' }}
      SOURCE_DIR: ${{ vars.SOURCE_DIR || '' }}
      DOCKER_DIR: ${{ vars.DOCKER_DIR || '' }}
      WEBUI_DIR:  ${{ vars.WEBUI_DIR  || '' }}
    steps:
      - uses: actions/checkout@v4

      - name: Resolve paths
        shell: bash
        run: |
          echo "INFRA_DIR=${INFRA_DIR:-$HOME/5gcore-deploy}"               >> "$GITHUB_ENV"
          echo "SOURCE_DIR=${SOURCE_DIR:-$HOME/Downloads/etrib5gc}"        >> "$GITHUB_ENV"
          echo "DOCKER_DIR=${DOCKER_DIR:-$HOME/Downloads/etrib5gc/docker}" >> "$GITHUB_ENV"
          echo "WEBUI_DIR=${WEBUI_DIR:-$HOME/Downloads/5gc-webui}"         >> "$GITHUB_ENV"

      # rsync SAU KHI infra-ensure tạo SSH config — exclude đủ để không xóa mất.
      - name: Sync manifest, scripts and observability configs
        shell: bash
        run: |
          set -e
          mkdir -p "$INFRA_DIR"
          rsync -a --delete \
            --exclude '.git/' \
            --exclude '.vagrant/' \
            --exclude '.venv/' \
            --exclude 'kubeconfig' \
            --exclude '.vagrant-ssh-config' \
            --exclude '.ansible-vagrant-inventory.yaml' \
            --exclude 'rendered/' \
            --exclude 'rendered-test/' \
            "$GITHUB_WORKSPACE"/ "$INFRA_DIR"/

      - name: Login to GHCR
        if: needs.detect-changes.outputs.need_build == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # LOGIC: DOCKER_DIR phải tồn tại trên runner. Nếu không, job fail rõ ràng
      # ngay từ đầu thay vì fail mơ hồ bên trong copybuild.sh.
      - name: Build & Push 5G Core Images
        if: needs.detect-changes.outputs.need_build == 'true'
        shell: bash
        run: |
          set -e

          if [ ! -d "$DOCKER_DIR" ]; then
            echo "ERROR: DOCKER_DIR=$DOCKER_DIR does not exist on this runner."
            echo "Set the DOCKER_DIR variable in GitHub repo settings."
            exit 1
          fi

          cd "$DOCKER_DIR"

          OWNER="${{ needs.infra-ensure.outputs.image_owner }}"
          TAG="${{ needs.infra-ensure.outputs.image_tag }}"
          [ -z "$OWNER" ] && OWNER="ninhduyhieu"
          [ -z "$TAG"   ] && TAG="latest"

          echo "Building with OWNER=$OWNER TAG=$TAG"

          chmod +x copybuild.sh
          ./copybuild.sh

          BUILD_OPTS="--memory=2g --cpu-shares=512"

          docker build $BUILD_OPTS \
            --target b5gc-base \
            -t "ghcr.io/${OWNER}/b5gc-base:${TAG}" \
            -t "ghcr.io/${OWNER}/b5gc-base:latest" \
            -f nf.Dockerfile .

          docker push "ghcr.io/${OWNER}/b5gc-base:${TAG}"
          docker push "ghcr.io/${OWNER}/b5gc-base:latest"

          for module in controller gateway nsm; do
            docker build $BUILD_OPTS \
              --build-arg B5GC_MODULE="$module" \
              -t "ghcr.io/${OWNER}/b5gc-${module}:${TAG}" \
              -t "ghcr.io/${OWNER}/b5gc-${module}:latest" \
              -f nf.Dockerfile .
            docker push "ghcr.io/${OWNER}/b5gc-${module}:${TAG}"
            docker push "ghcr.io/${OWNER}/b5gc-${module}:latest"
          done

          for nf in ausf udm pcf damf amf smf pran; do
            docker build $BUILD_OPTS \
              --build-arg B5GC_MODULE="$nf" \
              -t "ghcr.io/${OWNER}/b5gc-${nf}:${TAG}" \
              -t "ghcr.io/${OWNER}/b5gc-${nf}:latest" \
              -f nf.Dockerfile .
            docker push "ghcr.io/${OWNER}/b5gc-${nf}:${TAG}"
            docker push "ghcr.io/${OWNER}/b5gc-${nf}:latest"
          done

      # cd "$INFRA_DIR" trước vagrant — Vagrant tìm .vagrant/ từ CWD.
      # LOGIC sleep 120: chỉ sleep khi có VM bị restart (healed=true).
      - name: Pre-deploy cluster gate with auto-heal
        if: >
          needs.detect-changes.outputs.need_deploy == 'true' ||
          needs.detect-changes.outputs.need_monitoring == 'true'
        shell: bash
        run: |
          set -e
          export KUBECONFIG="$INFRA_DIR/kubeconfig"
          cd "$INFRA_DIR"

          echo "Checking K3s API /cacerts..."
          curl -k --fail https://192.168.58.11:6443/cacerts >/dev/null

          kubectl get nodes -o wide || true

          healed=false
          for vm in k3s-agent-1 k3s-agent-2; do
            if ! kubectl get node "$vm" \
                   -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}' \
                   2>/dev/null | grep -q True; then
              echo "$vm is NotReady → restart"
              vagrant halt -f "$vm" || true
              timeout 10m vagrant up "$vm" --no-provision
              vagrant ssh "$vm" -c \
                "sudo systemctl restart k3s-agent || sudo systemctl restart k3s || true"
              healed=true
            fi
          done

          if [ "$healed" = "true" ]; then
            echo "Waiting for healed nodes to settle..."
            sleep 120
          fi

          kubectl get nodes -o wide
          kubectl wait --for=condition=Ready node/k3s-server-1 --timeout=600s
          kubectl wait --for=condition=Ready node/k3s-agent-1  --timeout=900s
          kubectl wait --for=condition=Ready node/k3s-agent-2  --timeout=900s

      - name: Apply Kubernetes Manifests
        if: needs.detect-changes.outputs.need_deploy == 'true'
        shell: bash
        run: |
          set -e
          export KUBECONFIG="$INFRA_DIR/kubeconfig"
          cd "$INFRA_DIR"

          OWNER="${{ needs.infra-ensure.outputs.image_owner }}"
          TAG="${{ needs.infra-ensure.outputs.image_tag }}"
          [ -z "$OWNER" ] && OWNER="ninhduyhieu"
          [ -z "$TAG"   ] && TAG="latest"

          kubectl create namespace etri6g \
            --dry-run=client -o yaml | kubectl apply -f -

          ./scripts/render-ghcr-images.sh "$OWNER" "$TAG" rendered

          kubectl apply -f rendered/ctrl.yaml
          kubectl apply -f rendered/gw.yaml
          kubectl apply -f rendered/nsm.yaml
          kubectl apply -f rendered/mongo.yaml
          kubectl apply -f rendered/deploy-other.yaml
          kubectl apply -f rendered/pran.yaml

      - name: Wait for 5GC deployments with rollback
        if: needs.detect-changes.outputs.need_deploy == 'true'
        shell: bash
        run: |
          set -e
          export KUBECONFIG="$INFRA_DIR/kubeconfig"

          DEPLOYMENTS="ctrl-dep gateway-dep nsm-dep udr-mongo \
            ausf-dep udm-dep pcf-dep damf-dep \
            amf-10-100-agent1-dep amf-10-100-agent2-dep \
            smf-slice1-dep smf-slice2-dep pran-dep"

          failed=""
          for dep in $DEPLOYMENTS; do
            echo "Checking rollout: $dep"
            if ! kubectl rollout status "deployment/${dep}" \
                   -n etri6g --timeout=300s; then
              echo "Rollout failed: $dep"
              failed="$failed $dep"
            fi
          done

          if [ -n "$failed" ]; then
            echo "Rolling back:$failed"
            for dep in $failed; do
              kubectl rollout undo "deployment/${dep}" -n etri6g || true
            done
            kubectl get pods -n etri6g -o wide || true
            exit 1
          fi

      - name: Deploy Monitoring Stack (Loki / Alloy / Grafana)
        if: needs.detect-changes.outputs.need_monitoring == 'true'
        shell: bash
        run: |
          set -e
          export KUBECONFIG="$INFRA_DIR/kubeconfig"
          cd "$INFRA_DIR"

          kubectl create namespace observability \
            --dry-run=client -o yaml | kubectl apply -f -

          helm repo add grafana https://grafana.github.io/helm-charts || true
          helm repo update

          helm upgrade --install loki grafana/loki \
            -n observability \
            -f "$INFRA_DIR/observability/loki-values.yaml" \
            --wait --timeout 10m

          helm upgrade --install alloy grafana/alloy \
            -n observability \
            -f "$INFRA_DIR/observability/alloy-values.yaml" \
            --wait --timeout 10m

          helm upgrade --install grafana grafana/grafana \
            -n observability \
            -f "$INFRA_DIR/observability/grafana-values.yaml" \
            --wait --timeout 10m

          kubectl get pods -n observability -o wide

      # LOGIC: pkill bằng pattern đủ tốt cho môi trường dev local.
      # Lưu PID vào file để verify process thực sự lên sau khi start.
      - name: Restart Dashboard Application (WebUI)
        if: >
          needs.detect-changes.outputs.need_webui == 'true' ||
          github.event_name == 'workflow_dispatch'
        shell: bash
        run: |
          set -e

          pkill -f "uvicorn server:app" || true
          pkill -f "vite"               || true
          sleep 2

          cd "$WEBUI_DIR/backend"
          export PROJECT_DIR="$INFRA_DIR"
          export K8S_NAMESPACE=etri6g
          nohup uvicorn server:app --host 0.0.0.0 --port 8000 \
            > /tmp/webui-backend.log 2>&1 &
          echo $! > /tmp/webui-backend.pid

          cd "$WEBUI_DIR/frontend"
          nohup npm run dev -- --host 0.0.0.0 \
            > /tmp/webui-frontend.log 2>&1 &
          echo $! > /tmp/webui-frontend.pid

          sleep 5

          # Verify cả hai process thực sự đang chạy
          if ! kill -0 "$(cat /tmp/webui-backend.pid)" 2>/dev/null; then
            echo "ERROR: backend failed to start. Last log:"
            tail -20 /tmp/webui-backend.log
            exit 1
          fi
          if ! kill -0 "$(cat /tmp/webui-frontend.pid)" 2>/dev/null; then
            echo "ERROR: frontend failed to start. Last log:"
            tail -20 /tmp/webui-frontend.log
            exit 1
          fi
          echo "WebUI backend PID=$(cat /tmp/webui-backend.pid) OK"
          echo "WebUI frontend PID=$(cat /tmp/webui-frontend.pid) OK"

      # kubectl get nodes dùng || true — nếu cluster down thì step verify
      # không nên fail toàn bộ pipeline (deploy đã xong rồi).
      - name: Verify current state
        shell: bash
        run: |
          export KUBECONFIG="$INFRA_DIR/kubeconfig"

          echo "=== Nodes ==="
          kubectl get nodes -o wide || true

          echo "=== 5G Pods ==="
          kubectl get pods -n etri6g -o wide || true

          echo "=== Observability Pods ==="
          kubectl get pods -n observability -o wide || true

          echo "=== WebUI Processes ==="
          pgrep -af "uvicorn server:app|vite" || true
